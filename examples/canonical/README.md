# Canonical Minimal Examples

One minimal parameter file per supported mode:

- `uniform_minimal.txt`
- `invitro_bind_minimal.txt`
- `activity_map_minimal.template.txt` - requires DICOM paths to be filled in
